"// src/components/Login.jsx\nimport React, { useState, useEffect, useRef } from 'react';\nimport { Landmark, ArrowRight, ShieldCheck, RefreshCw, X, Loader2, KeyRound } from 'lucide-react';\nimport { loginSendOtp, loginVerifyOtp, forgotSendOtp, forgotVerifyOtp } from '../services/api';\n\n/**\n * Login\n *\n * Renders the admin authentication screen, verifying credentials,\n * executing canvas CAPTCHA checks, and coordinating 2FA/Password Reset modals.\n *\n * @param  {Object}   props - React component properties.\n * @param  {Function} props.onLoginSuccess - Callback executed on verified authentication.\n * @param  {Function} props.showToast - Callback to display notifications in UI.\n * @returns {React.Component} - Login page layout structure.\n */\nexport default function Login({ onLoginSuccess, showToast }) {\n  const [username, setUsername] = useState('');\n  const [password, setPassword] = useState('');\n  const [captchaInput, setCaptchaInput] = useState('');\n  const [currentCaptcha, setCurrentCaptcha] = useState('');\n  \n  // Loading and error states\n  const [isSubmitting, setIsSubmitting] = useState(false);\n  const [errorMessage, setErrorMessage] = useState('');\n  \n  // OTP states\n  const [showOtpModal, setShowOtpModal] = useState(false);\n  const [otpInput, setOtpInput] = useState('');\n  const [isVerifyingOtp, setIsVerifyingOtp] = useState(false);\n  const [otpError, setOtpError] = useState('');\n\n  // Forgot Password modal states\n  const [showForgotModal, setShowForgotModal] = useState(false);\n  const [forgotStep, setForgotStep] = useState(1); // 1: Email, 2: OTP, 3: Success\n  const [forgotEmail, setForgotEmail] = useState('');\n  const [forgotOtp, setForgotOtp] = useState('');\n  const [forgotError, setForgotError] = useState('');\n  const [isForgotLoading, setIsForgotLoading] = useState(false);\n\n  const canvasRef = useRef(null);\n\n  /**\n   * generateCaptcha\n   *\n   * Renders random alphanumeric strings inside an HTML5 Canvas to prevent automated spamming.\n   */\n  const generateCapt
<truncated 18674 bytes>
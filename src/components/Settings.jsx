"// src/components/Settings.jsx\nimport React, { useState, useEffect } from 'react';\nimport { Settings, CircleDollarSign, Mail, Edit, Check, X, ShieldAlert, Loader2 } from 'lucide-react';\nimport { getSettings, saveMaintenanceAmount, saveFlatEmails, settingsSendOtp, settingsVerifyOtp } from '../services/api';\n\n/**\n * SettingsComponent\n *\n * Renders the Settings Portal view, permitting default amount changes, \n * owner email configurations, inline edits, and staged OTP validations.\n *\n * @param  {Object}   props - React properties.\n * @param  {number}   props.defaultAmount - Default maintenance charge value.\n * @param  {Function} props.setDefaultAmount - State setter trigger.\n * @param  {Object}   props.emailMap - Flat email configurations.\n * @param  {Function} props.setEmailMap - State setter trigger.\n * @param  {Function} props.showToast - Trigger to display notifications.\n * @param  {boolean}  props.isDemoMode - Indicates if running inside demo auth scope.\n * @returns {React.Component} - Settings View.\n */\nexport default function SettingsComponent({ defaultAmount, setDefaultAmount, emailMap, setEmailMap, showToast, isDemoMode }) {\n  const [maintenanceInput, setMaintenanceInput] = useState(defaultAmount.toString());\n  const [isSavingAmount, setIsSavingAmount] = useState(false);\n\n  // Email management states\n  const [emailSearchQuery, setEmailSearchQuery] = useState('');\n  const [emailSuggestions, setEmailSuggestions] = useState([]);\n  const [showEmailSuggestions, setShowEmailSuggestions] = useState(false);\n  const [selectedFlat, setSelectedFlat] = useState(null);\n  \n  // Staging changes\n  const [flatEmailsList, setFlatEmailsList] = useState([]);\n  const [editingIndex, setEditingIndex] = useState(null);\n  const [editingValue, setEditingValue] = useState('');\n  const [pendingChanges, setPendingChanges] = useState({}); // Stores key as flatId-idx, value as new email\n\n  // OTP confirmation states\n  const [otpType, setOtpType] = useState(null); // 'amount' or 'emails'\n  const [sho
<truncated 17320 bytes>
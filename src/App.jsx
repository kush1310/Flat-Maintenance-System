"// src/App.jsx\nimport React, { useState, useEffect, useRef } from 'react';\nimport {\n  Landmark, LayoutGrid, History as HistoryIcon, Wallet,\n  Settings as SettingsIcon, LogOut, Bell, Menu, X, ArrowLeft,\n  CheckCircle2, Receipt, AlertCircle, Banknote, Smartphone,\n  Building2, FileCheck2\n} from 'lucide-react';\nimport { checkAuth, logout, getFlats, getBudget, getSettings } from './services/api';\nimport Login from './components/Login';\nimport Dashboard from './components/Dashboard';\nimport History from './components/History';\nimport Budget from './components/Budget';\nimport SettingsComponent from './components/Settings';\n\n/**\n * App\n *\n * Root React application controller coordinating session authentication state,\n * data pipelines, tab routing, sidebar states, modal flows, and preloaders.\n *\n * @returns {React.Component} - Root App layout.\n */\nexport default function App() {\n  const [isAuthenticated, setIsAuthenticated] = useState(false);\n  const [user, setUser] = useState('');\n  const [loading, setLoading] = useState(true);\n\n  // Application data states\n  const [flatsData, setFlatsData] = useState({});\n  const [emailMap, setEmailMap] = useState({});\n  const [budgetData, setBudgetData] = useState({ totalFunds: 0, transactions: [] });\n  const [defaultAmount, setDefaultAmount] = useState(500);\n\n  // Navigation\n  const [activeTab, setActiveTab] = useState(null);\n  const [isSidebarOpen, setIsSidebarOpen] = useState(false);\n\n  // Preloader states\n  const [loginPreloader, setLoginPreloader] = useState(false);\n  const [logoutPreloader, setLogoutPreloader] = useState(false);\n\n  // Modal states\n  const [logoutConfirmOpen, setLogoutConfirmOpen] = useState(false);\n  const [successModal, setSuccessModal] = useState({ open: false, title: '', subtitle: '' });\n  const [receiptModal, setReceiptModal] = useState({ open: false, data: null });\n\n  // Notification feed (each entry is a receipt/activity object)\n  const [notifications, setNotifications] = useState([]);\n\n  // Minor toast st
<truncated 28374 bytes>